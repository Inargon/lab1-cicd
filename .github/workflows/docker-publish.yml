name: Docker Build and Push

on:
  push:
    branches: [ "main" ]

# 权限配置：允许写入 GitHub Packages
permissions:
  contents: read
  packages: write

jobs:
  # 第一步：测试任务
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm install
        
      - name: Run tests
        run: npm test

  # 第二步：构建并推送任务
  build-and-push:
    needs: test # 依赖测试任务，测试通过才执行
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # 🚨 关键修改点 🚨
          # 1. 将 ${{ github.repository_owner }} 替换为小写的 "inargon"
          # 2. 同时保留 latest 和 commit sha 两个标签（满足进阶作业满分要求）
          tags: |
            ghcr.io/${{ github.repository_owner }}/ci-cd-practice:latest
            ghcr.io/${{ github.repository_owner }}/ci-cd-practice:${{ github.sha }}
